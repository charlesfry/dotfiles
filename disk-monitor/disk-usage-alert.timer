[Unit]
Description=Periodic disk usage check (every 4h)

[Timer]
# 00:00, 04:00, 08:00, 12:00, 16:00, 20:00 -> 6x/day
OnCalendar=*-*-* 00/4:00:00
# Catch up a missed run (e.g. machine was asleep at the scheduled time)
Persistent=true
RandomizedDelaySec=2min

[Install]
WantedBy=timers.target
